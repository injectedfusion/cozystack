{{- if not (eq .Values.topology "Client") }}
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: {{ .Release.Name }}-db
  labels:
    sharding.fluxcd.io/key: tenants
spec:
  chartRef:
    kind: ExternalArtifact
    name: cozystack-seaweedfs-application-default-seaweedfs-db
    namespace: cozy-system
  interval: 5m
  timeout: 10m
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: -1
  # `poller` waitStrategy is required for healthCheckExprs to be evaluated.
  # Without it, the HR flips Ready as soon as helm install applies the Cluster CR,
  # before CNPG has bootstrapped postgres.
  waitStrategy:
    name: poller
  # CNPG sets Ready=False with reason=ClusterIsNotReady during normal bootstrap,
  # so a plain Ready=False predicate flags the HR failed within seconds. Treat
  # the bootstrap reason as in-progress; reserve `failed` for other Ready=False
  # reasons CNPG might add in the future. The HR `timeout:` is the real backstop.
  healthCheckExprs:
    - apiVersion: postgresql.cnpg.io/v1
      kind: Cluster
      inProgress: has(status.conditions) && status.conditions.exists(e, e.type == 'Ready' && e.status == 'False' && e.reason == 'ClusterIsNotReady')
      failed:     has(status.conditions) && status.conditions.exists(e, e.type == 'Ready' && e.status == 'False' && e.reason != 'ClusterIsNotReady')
      current:    has(status.conditions) && status.conditions.exists(e, e.type == 'Ready' && e.status == 'True')
  valuesFrom:
  - kind: Secret
    name: cozystack-values
  values:
    db:
      replicas: {{ .Values.db.replicas }}
      resources: {{- include "cozy-lib.resources.defaultingSanitize" (list .Values.db.resourcesPreset .Values.db.resources $) | nindent 8 }}
      size: {{ .Values.db.size }}
      {{- with .Values.db.storageClass }}
      storageClass: {{ . }}
      {{- end }}
{{- end }}
